export type TMenuItemLink = {
  name: string,
  link: string
};
export type TMenuItem = {
  name: string,
  image: string,
  ingredients: string,
  category: string,
  weghit: number,
  price: number
};
